package ch6_SelfTest;

public class Exercise9 {
    /*
    you cannot reference an instance variable within the definition of a static method,
     because a static method can be invoked without an object,
      and so there are no instance variables.

     */
}
